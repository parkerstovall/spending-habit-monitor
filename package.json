{
  "name": "spending-habit-monitor",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "dev": "concurrently \"npm run db\" \"npm run api\" \"npm run app\"",
    "app": "cd app && npm run dev",
    "api": "cd api && dotnet run",
    "db": "docker compose up -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "parker.stovall",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
